# 海老さんローグライク プレイガイド

この文書では、Windows版「海老さんローグライク」の起動方法、操作方法、基本ルールを説明します。

## ゲームの目的

主人公の「海老さん」を操作して、ランダムに生成されるダンジョンを探索するターン制ローグライクです。敵を倒して成長し、道具や装備を活用しながら、各フロアにある階段を見つけて先へ進みます。

海老さんのHPが0になると冒険は終了し、中断データも削除されます。状況に応じて戦うか逃げるかを選び、できるだけ深い階層を目指してください。

## Windows版の起動方法

1. `ebirogue.exe` と `img` フォルダーが同じフォルダー内にあることを確認します。
2. `ebirogue.exe` をダブルクリックします。
3. Windows Defender SmartScreenが表示された場合は、発行元とファイルの場所を確認してから「詳細情報」→「実行」を選択します。

PowerShellから起動する場合は、リポジトリのフォルダーへ移動して実行します。

```powershell
cd "C:\Users\ken\賢舟の部屋\プログラム関係\ebirogue"
.\ebirogue.exe
```

画像は実行時に `img` フォルダーから読み込まれるため、`ebirogue.exe` だけを別の場所へ移動しないでください。

## 基本操作

### マップ上

| キー | 操作 |
|---|---|
| 矢印キー | 上下左右へ1マス移動 |
| `Shift`＋矢印キー | 斜めへ1マス移動 |
| `X`＋矢印キー | ダッシュ。部屋の入口、通路の分岐、敵や道具などの危険を検知すると停止 |
| `A`＋矢印キー | 移動せず、その方向へ向きを変える |
| `Z` | 向いている方向へ通常攻撃。空振り時は正面の未発見の罠も調べる |
| `X`＋`Z` | 足踏み。その場から動かず1ターン進める |
| `D` | 装備中の矢を撃つ |
| `A` | 押している間、矢・投擲・杖の射線と到達地点を表示 |
| `Space` | 隣接する扉を開く |
| `C` | メニューを開く。ウィンドウ表示中はすべて閉じる |
| `H` | ヘルプを開く |
| `L` | メッセージ履歴を開く |

### メニューとウィンドウ

| キー | 操作 |
|---|---|
| 矢印キー | 項目の選択、ページ移動、スクロール |
| `Z` | 決定 |
| `X` | キャンセル、前の画面へ戻る |
| `C` | 開いているウィンドウをすべて閉じる |

`C` メニューには「道具」「足元」「設定」「中断」「ヘルプ」があります。「足元」では現在のマスにある道具を拾ったり、所持品と交換したりできます。「設定」ではフルスクリーンとミニマップ表示を切り替えられます。

### 道具ウィンドウ

| キー | 操作 |
|---|---|
| `F` | 所持している道具カテゴリだけを順番に絞り込み |
| `S` | カテゴリ・種類順に整頓。同じ種類の矢は1つにまとめる |
| `N` | 未識別の道具に任意の名前を付ける |

名前入力では、`Z` で文字入力、`X` で1文字削除、`Enter` で決定します。空欄のまま決定すると設定済みの名前を消去します。

## ターン制のルール

ゲーム内の時間は、海老さんが行動したときに進みます。移動、攻撃、足踏み、矢を撃つ、道具を使うなどで1ターンを消費し、その後に敵が行動します。メニューを見たり向きだけを変えたりしても、通常はターンを消費しません。

敵に囲まれた状態で不用意に連続行動すると、一度に複数の攻撃を受けます。通路で一対一の状況を作る、扉や地形を使って距離を取る、足踏みで敵を近づける、といった位置取りが重要です。

鈍足中は敵が2回行動します。倍速中の敵も1ターンに2回行動するため、通常時より早めに距離を取ってください。

## 探索と視界

- マップ、敵、道具、罠、階段は冒険ごとに配置が変わります。
- 部屋の中では部屋全体を見渡せますが、通路では周囲しか見えません。
- ミニマップには探索済みの地形が記録されます。表示の有無は設定で変更できます。
- 罠は最初は隠れていることがあります。罠の上を歩くと作動する可能性があり、正面へ空振り攻撃するとそのマスを調べられます。
- フロアに長く滞在しすぎると風の警告が発生します。探索を終えたら階段へ向かいましょう。

階段の上に乗ると次のフロアへ進むか確認されます。フロアを移動するとオートセーブされます。

## 戦闘と成長

- 通常攻撃は向いている方向の隣接マスが対象です。
- 通常武器の攻撃は外れることがあります。必中の剣など、固有能力を持つ装備もあります。
- 矢、投擲道具、杖は直線上の対象へ届きます。射線表示で到達地点を確認できます。
- 敵を倒すと経験値を獲得し、一定値に達するとレベルアップします。
- 武器、防具、アクセサリを装備すると能力値や特殊効果が変化します。
- 敵がほかの敵を倒すと、同系統の上位種へ成長することがあります。

HPが最大値より少なく、満腹度が残っている場合は、5ターンごとにHPが1回復します。安全な場所で足踏みすると回復できますが、その間も敵や満腹度は動くため周囲を確認してください。

## 満腹度とパワー

満腹度は行動を続けると少しずつ減少します。食料を食べて回復してください。満腹度が0になると自然回復が止まり、行動するたびにHPが1減ります。

パワーは攻撃力に影響します。毒などで下がることがあるため、回復手段を確保しておくと安全です。

## 道具と装備

所持できる道具は最大20個です。床の道具は「足元」メニューから拾えます。所持品が満杯の場合は、足元の道具と所持品を交換できます。

- 武器・防具・矢・アクセサリは装備して使用します。
- 薬、食料、カードなどは使用すると原則として消費されます。
- 杖には使用回数があります。
- 壺には道具を出し入れできます。壺を投げて割ると中身が飛び出します。
- 未識別の道具は、実際に使うなどして効果を確かめる必要があります。推測した名前は `N` で記録できます。
- 強化値が `-1` の武器や防具は呪われています。呪われた装備は通常の方法では外せません。

道具には特殊な使い方を持つものがあります。たとえば、聖域のカードは床に置くと効果を発揮し、ジェノサイドのカードは敵へ投げ当てて使用します。詳しくはゲーム内ヘルプや各道具の「説明」を確認してください。

## 状態異常

| 状態 | 効果 |
|---|---|
| 毒 | 行動するたびにHPが2減る |
| 鈍足 | 行動が遅くなり、敵が2回行動する |
| 倍速 | 通常の2倍の速さで行動できる |
| 睡眠 | しばらく行動できない |
| 混乱 | 移動や攻撃の方向がランダムになる |
| 目潰し | 敵、道具、ミニマップが見えなくなる |
| 金縛り | 行動できない。攻撃を受けると解除される |
| 封印 | 特殊能力を使用できない |
| 口封じ | カード、薬、食料を口にできない |

残りターンがある状態異常は時間経過で回復します。現在の状態は画面左側の「状態」欄で確認できます。

## セーブと終了

安全に終了するには、`C` メニューの「中断」を選び、「中断する」を決定します。中断データを保存してゲームが終了し、次回起動時に同じ冒険から自動的に再開します。攻撃やアニメーションなどの行動処理中は中断できません。

- 中断・オートセーブ: `ebirogue_save.json`
- 設定: `ebirogue_settings.json`

これらはゲームを起動したフォルダーに保存されます。中断データは死亡時に削除されます。破損して読み込めない場合も破棄され、新しい冒険が始まります。

## 最初の冒険で意識すること

1. 部屋を一周して道具と階段の位置を確認します。
2. 敵とは通路で一対一になるように戦います。
3. HPが減ったら、安全を確認してから足踏みで回復します。
4. 満腹度が少なくなる前に食料を確保します。
5. 未識別の道具を試すときは、逃げ道やHPに余裕を持たせます。
6. 危険になったら探索に固執せず、階段で次のフロアへ進みます。
